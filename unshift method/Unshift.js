const Animal = ["Bufello", "cow", "elephant", "oxx"];

//The unshift() method adds one or more elements to the beginning 
//of an array and returns the new length of the array.
Animal.unshift("cow");

console.log(Animal);
